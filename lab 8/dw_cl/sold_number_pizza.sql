alter session set current_schema = DW_DATA;

CREATE TABLE DIM_SOLD_NUMBER_PIZZA(
    NUMBER_ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,     
    AMOUNT     NUMBER,
    CONSTRAINT "PK_T.DIM_SOLD_NUMBER_PIZZA" PRIMARY KEY(NUMBER_ID) 
);

--DROP TABLE DIM_SOLD_NUMBER_PIZZA