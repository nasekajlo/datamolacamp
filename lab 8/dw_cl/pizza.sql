alter session set current_schema = DW_DATA;

CREATE TABLE DIM_PIZZA(
    PIZZA_ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,     
    PIZZA_NAME       VARCHAR (20),
    PIZZA_SIZE      NUMBER,
    PIZZA_COST    NUMBER,
    PIZZA_RANK    NUMBER,
    CONSTRAINT "PK_T.DIM_PIZZA" PRIMARY KEY(PIZZA_ID) 
);

--DROP TABLE DIM_PIZZA