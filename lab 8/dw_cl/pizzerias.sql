alter session set current_schema = DW_DATA;

CREATE TABLE DIM_PIZZERIA(
    PIZZERIA_ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,     
    PIZZERIA_NAME               VARCHAR2(20), 
    PIZZERIA_ADDRESS            VARCHAR2(100),
    PIZZERIA_PHONE          VARCHAR2(15),
    CONSTRAINT "PK_T.DIM_PIZZERIA" PRIMARY KEY(PIZZERIA_ID) 
);


--DROP TABLE DIM_PIZZERIAS;